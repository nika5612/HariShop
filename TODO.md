# TODO

## UI/UX Polish (Theme/Contrast + Typography)

- [ ] Unify typography scale and reduce oversized headings in Home (luxHome) and in Order/Checkout pages.
- [ ] Improve text contrast on components that currently use white/green/gray mixes (header + some pages).
- [ ] Replace hardcoded dark backgrounds in Product/Order/Checkout with theme variables (where safe) to avoid unreadable combinations.
- [ ] Validate layouts on mobile breakpoints (<=991px, <=575px).
- [ ] Quick manual QA: Home, Product listing, Order detail, Checkout, Admin order list.

## Implemented (UI layout/contrast foundation)
- [x] Identified main cause candidates: hardcoded inline colors + Home page uses embedded STYLE string (luxHome) instead of global theme variables.
- [x] Chốt scope theo yêu cầu: chỉ tăng độ tương phản, tỉ lệ và bố cục chữ (không đổi layout lớn).



